  a := 1;
  b := 2;
  c := @set(a,b);
  d := @set(@tuple(1,2),@tuple(3,4));
  e := @is-function(c);
  f := @is-function(d);
  g := @equal(c,@set(a,2));
  h := @apply-function(d,a);
  i := @tuple(@tuple("st0","sy0"),@tuple("st1","sy1","L"));
  j := @set(i);
  l := @set(@tuple(0,"sy1"),@tuple(1,"sy2"));
  o := @apply-function(d,b);
  q := -1;
